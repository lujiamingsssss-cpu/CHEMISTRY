# Repository Engineering Rules

本文件适用于整个仓库，是 Codex 和其他编码代理进入本项目后必须自动执行的常驻工程规则。

## 1. 权威与适用范围

指令优先级固定为：

1. 用户当前明确要求；
2. 本 `AGENTS.md`；
3. 与当前任务匹配的 `CURRENT_WORK.md`；
4. 已接受且仍有效的架构决策记录；
5. `docs/ENGINEERING_PLAYBOOK.md`；
6. 当前代码、测试和 Git 可验证事实。

旧的项目说明、状态和交接文档只记录历史业务内容与既有实现事实，不自动支配新的功能开发或 Bug 修复。不得因历史文档存在而跳过本文件规定的工程流程。

## 2. 每次任务的自动启动流程

每个任务必须先运行 `git rev-parse --show-toplevel` 确定当前实际 checkout 根目录；后续仓库路径均以该结果为基准。在修改任何文件前必须：

1. 优先读取当前 checkout 根目录的 `AGENTS.md`；不得把主仓、另一个 Worktree 或其他 checkout 的 `AGENTS.md` 当作当前任务权威；
2. 对开发、修复、重构、依赖、数据或配置变更，读取当前 checkout 的 `docs/ENGINEERING_PLAYBOOK.md`；
3. 检查 `git status --short`，保护用户已有改动；
4. 检查当前 checkout 根目录是否存在 `CURRENT_WORK.md`；只要存在 `CURRENT_WORK.md`，就必须先运行 `python scripts/check_current_work_hygiene.py --repo .`；门禁失败时只允许由该文件所属任务修复状态载体，不得进入实现或下一里程碑；
5. 不得主观判断 `CURRENT_WORK.md` 与当前任务“不匹配”后跳过 checker；若该文件属于另一任务，应停止或切换到正确 checkout，不得覆盖、修改或继续该任务；不得新增“继承但不属于当前 checkout”、“外来 CURRENT_WORK”或类似豁免逻辑；
6. 阅读与请求直接相关的代码、测试和配置；
7. 使用最小任务路由：先判断是否只读；需要改动时只分为 Bug、Feature 或 Task；再分别判断是否属于长任务、是否属于高风险；
8. 先确定范围、非目标、验收证据和停止条件，再开始实现。

只读解释或状态查询不得擅自修改代码、文档、外部系统或 Git 历史。

### Git 工作树路径权威

- 工作树绝对路径必须来自目标仓库的 `git worktree list --porcelain`：按记录的分支唯一匹配，并原样复制对应 `worktree` 行；`CURRENT_WORK.md` 中的路径只用于交叉核验。禁止把仓库路径与 `.worktrees` 手工拼接，也不得凭记忆、目录命名习惯或聊天文本重新构造绝对路径。
- 生成跨会话续接提示、写入 `CURRENT_WORK.md` 或执行工作树命令前，必须验证该路径存在，且 `git -C <path> branch --show-current`、`git -C <path> rev-parse --show-toplevel` 与目标分支及注册记录一致。
- 用户、prompt 或交接文本给出的路径与唯一 Git 注册工作树相比，只存在漏写或多写目录分隔符、斜杠方向或大小写差异，且目标分支唯一一致时，视为路径转录错误：使用 Git 注册路径并简短说明已归一化，不因纯路径格式差异机械停止。
- 只有存在多个候选，或分支、HEAD、任务状态或实际目录发生无法解释的实质漂移时才停止。
- main 中提交的 `AGENTS.md` 是未来新 Worktree 的长期基线；现有 Worktree 仍读取自己的 checkout 副本，不通过软链接、junction、启动钩子或外部共享文件统一物理副本。

### 最小任务路由

- **Read-only**：解释、调研、诊断、审查或状态报告，不修改文件或外部状态；
- **Bug**：已有行为与明确预期不一致；
- **Feature**：增加或改变用户可观察能力；
- **Task**：重构、依赖、测试、文档、构建、数据或运行操作等其他改动。

选择一种改动类型后只追加两个是非判断：是否为长任务、是否为高风险。任一为“是”时创建 `CURRENT_WORK.md`；高风险任务还必须设置人工批准点和回滚路径。业务优先级和紧急度由用户决定，Codex 不建立评分体系。除非判断会触发建档、审批或停止，否则无需向用户输出分类报告。

### 疑难问题升级门禁：成熟经验优先

“不能快速解决”不按固定分钟数判断。出现以下任一情况即触发本门禁：一次有界的聚焦诊断或最小实验仍不能确定根因或下一项有效验证；同一解决路径连续失败两次；问题涉及不熟悉的技术、版本兼容、外部集成或可能已有成熟实现的通用能力。

触发后必须暂停猜测性修改、重复试错和自写替代实现，先参考成熟技术经验，顺序为：仓库现有代码、测试、配置和 Git 历史；适用版本的官方文档、参考实现、变更记录和上游已知问题；维护良好的成熟开源实现、行业通行模式或同类项目实践。必须核对版本与适用条件、维护状态、许可证、依赖与运行成本、安全边界，并用最小复现或上游最小示例验证候选方案是否适用于当前问题。

只有固定失败案例或验收证据证明成熟方案不适用、不足或成本不可接受，并记录被否决原因后，才允许编写满足当前案例的最小自定义实现。检索未命中不等于不存在成熟方案，不得因搜索困难而直接造轮子；也不得盲目复制外部方案、用权威来源替代本地复现和测试，或绕过新增依赖、架构变化及高风险操作原有的批准门禁。

## 3. CURRENT_WORK.md 强制规则

满足以下任一长任务条件，或被最小任务路由判定为高风险时，必须在修改实现前，根据工程手册模板创建根目录 `CURRENT_WORK.md`：

- 用户明确称为长任务、多阶段任务或要求跨会话持续完成；
- 无法在一次完整工作回合内可靠完成并验证；
- 同时涉及三个或以上模块或子系统；
- 同时包含调研、设计、实现、迁移、数据处理或验收中的多个阶段；
- 涉及数据库、索引、资料迁移、外部服务、权限、安全边界或架构调整；
- 存在两个或以上人工批准点；
- 失败可能破坏用户数据、正式索引、发布状态或当前可用版本；
- 需要由后续 Codex 任务继续；
- 当前上下文不足以让后续任务仅凭 Git 差异安全接手。

执行要求：

- 一个分支或工作树同时只能有一个 `CURRENT_WORK.md`；
- `CURRENT_WORK.md` 必须通过仓库内置卫生门禁：超过 12 KiB 发出警告，超过 16 KiB 或 120 行失败；必须包含当前范围、非目标、权威状态、批准与回滚、复杂度预算、临时产物生命周期及唯一下一动作；不得包含历史里程碑流水账、命令日志、瞬时 PID 或多代资产状态；
- `docs/superpowers/plans`、`docs/superpowers/reviews` 或其他目录不得保留第二份任务状态或交接文档；长期价值必须迁移到规则、测试、ADR 或已验证运行手册；
- 已有文件与当前请求相关时，先读取并从记录的下一动作继续；
- 已有文件与当前请求无关时，不得覆盖、修改或跳过卫生门禁，必须停止或切换到正确 checkout；
- 每完成一个里程碑、发现关键根因、到达人工批准点或准备跨会话交接时更新；
- 不记录每条命令，只记录范围、证据、决定、状态、失败和下一动作；
- 发现会影响后续执行的坑时必须及时写入 `CURRENT_WORK.md`，不能等到会话结束才凭记忆补写。记录必须说明现象与触发条件、已确认根因或证据、失败尝试及其失败原因、不得重复的做法、正确替代路径，以及原结论何时可能失效或允许重新验证；
- 任务完成前，将长期价值转移到代码、测试、ADR或确有必要的运行手册；
- 任务范围发生切换时，删除已完成或已暂停范围的过程记录，只保留当前未完成范围、必要门禁和唯一下一动作；不得在同一文件中累积历次任务流水账；
- 整个长任务通过新鲜验证后，在同一收口变更中删除 `CURRENT_WORK.md`，不建立永久滚动状态文档或归档副本。

### 跨会话续接与有序中断

`CURRENT_WORK.md` 是长任务跨会话续接的唯一权威状态载体。聊天记录、模型记忆、旧计划和历史提交只能用于辅助核验，不得取代其中已落盘的任务状态。

- 长任务必须始终保持可接管状态。每完成一个里程碑、获得人工批准、发现关键失败、改变范围，或判断当前会话可能无法完成时，及时记录当前分支、工作树和提交基线，本任务改动与用户既有改动，已完成里程碑，最近有效验证，批准的对象、范围和条件，阻塞与风险，以及一个具体且可直接执行的唯一下一动作；
- 跨会话交接必须把仍有效的坑写到下一会话无需依赖聊天记录即可避开的程度。新会话接管时必须先阅读这些坑；除非出现足以推翻原结论的新证据或适用条件已经变化，否则不得重复已确认失败的路径；
- 用户在长任务尚未完成时提出“做好跨会话交接并停止”或同义要求，无论 Codex 正在执行，还是当前回合或阶段已经停止并等待用户，都视为有序中断与交接请求。存在活动原子操作时，Codex 必须停止启动新的工作或里程碑，自行选择最近的安全中断点，完成、取消或回滚当前原子操作；不存在活动操作时，直接核验当前状态。随后把 `CURRENT_WORK.md` 更新为可接管状态，再停止。不得为了交接进入下一里程碑，也不得执行记录的唯一下一动作；
- 新会话收到“继续当前长任务”或同义要求时，必须核验请求与任务目标、分支和工作树、当前 Git 状态、关键文件与证据、下一动作及其所需批准。核验一致后直接执行 `CURRENT_WORK.md` 中的唯一下一动作，不重新要求确认，也不重复已经完成的调研、设计或验证；
- 只有明确写入 `CURRENT_WORK.md`，并记录批准对象、允许范围和适用条件的人工批准，才能由后续会话继承。未记录、含糊或条件已经变化的批准不得继承；“继续任务”本身不构成新增范围、发布、部署、外部发送或其他未记录高风险操作的授权；
- 若活动文件属于另一任务，分支、工作树或关键文件存在无法解释的漂移，下一动作不唯一、过时或缺少必要输入，批准条件不再成立，或继续执行会扩大原范围，必须停止并向用户报告，不得机械续做；
- 若会话意外结束，只以最近一次已落盘状态为准。未写入的聊天进展不得视为已完成，后续会话必须重新核验。

任务结束时必须清理坑记录：只对当前任务、临时环境或已经失效的内容随 `CURRENT_WORK.md` 一并删除；只有适用条件稳定、未来任务很可能重复遇到且复发代价明显的坑，才提炼为长期资产，迁移到最接近事实的代码、回归测试、稳定工程规则、必要 ADR 或已验证运行手册。不得为保存坑而保留已完成的 `CURRENT_WORK.md`，也不得新建避坑归档或历史流水账；长期坑失效后必须从长期载体中更新或删除。

## 4. 新功能流程

新功能必须按顺序执行：

1. 明确用户、真实问题、成功标准、范围和非目标；
2. 检查现有实现和成熟方案；触发疑难问题升级门禁时，必须先完成成熟经验调研与最小适用性验证，避免重复造轮子；
3. 对会改变行为或界面的方案先获得必要审批；
4. 在实现前建立可执行验收案例或失败测试；
5. 实现满足案例的最小变更；
6. 运行相关测试、全量回归及与风险相称的真实动态验证；
7. 审计是否引入无证据支持的依赖、抽象、服务或未来接口；
8. 只有验证证据完整时才能声称完成。

不得为了未来可能需要而预建平台、第二套实现、未使用接口或复杂扩展层。

## 5. Bug 修复流程

Bug 修复必须按顺序执行：

1. 稳定复现用户报告的现象；
2. 收集证据并定位根因，不凭症状猜修复；
3. 写出能够暴露原问题的失败回归测试；
4. 确认测试因正确原因失败；
5. 实施只针对根因的最小修复；
6. 运行回归测试并确认转绿；
7. 运行相关测试和全量测试；
8. 按风险进行真实浏览器、真实模型、文件系统、数据库或端到端验证；
9. 检查是否出现同类错误的其他入口。

禁止通过删除测试、放宽断言、改变原验收目标、盲目重试或吞掉异常来制造表面通过。需求确实改变时，必须先说明变化并获得确认。

定位或修复期间一旦触发疑难问题升级门禁，必须暂停继续堆叠补丁，先核对官方资料、上游已知问题和成熟实现，再决定采用、薄封装、组合或最小自建。

## 6. 证据、专业判断与安全边界

- 检索未命中不等于原始资料不存在；
- 引用真实不等于引用支持当前主张；
- 高风险参数必须与适用条件、测试方法、来源和人工核验事实绑定；
- 模型输出不得绕过本地证据门禁；
- 资料、安全、合规和专业化工判断不得由开发人员或模型代签；
- 涉及不可恢复写入、生产数据、正式索引、外部发送、发布或权限变更时，先确认授权和回滚路径；
- 清理与删除必须以目标的绝对路径、归属、内容范围、可重建性和恢复方式为依据；不得仅因目录名为临时、缓存、测试产物或已被 Git 忽略，就推定其中全部内容可删除。共享或混合目录只清理本轮明确创建的文件，化工资料、正式索引、客户数据、审阅记录及不可恢复产物未经明确授权不得删除；详细操作门禁见工程手册；
- 密钥、凭据、私有全文和敏感客户资料不得写入代码、测试、Markdown、日志或对话输出。

## 7. 变更规模与复杂度

- 一次变更只解决一个清晰问题；
- 大型重构与功能或 Bug 修复分开；
- 先使用现有组件和接口，只有固定失败案例证明不足时才增加新组件；
- 新依赖必须说明现有方案为何不足、维护状态、许可证、成本和回退方式；
- 阶段完成后立即停止，不自动进入未获授权的下一阶段；
- 用户要求保留未来扩展能力时，保留已被当前调用方或测试证明有价值的接口，但不启用空平台。

## 8. 文档准入与生命周期

普通功能和普通 Bug 不创建永久 Markdown。其意图由当前任务或 Issue 记录，行为由测试记录，状态由 Git/PR 记录。

只有以下情况允许新增永久文档：

- 所有未来任务都会反复使用的工程规则；
- 重大且难以撤销的技术决定；
- 已真实执行成功且以后会重复的人工运行流程；
- 不记录将很可能导致严重错误再次发生。

ADR 按工程手册的最小准入条件执行，仅在确有重大且难以撤销的技术决定时按需创建。运行手册必须在流程至少真实成功一次后编写。不得为每个阶段、对话或修复创建新的状态、交接、总结或计划文件。

任务完成时必须在同一收口中处理相关文档：删除状态、交接、临时计划、已完成清单、过程日志和可重建验收产物；把仍有长期价值的约束迁移到代码、测试、ADR、稳定规范或已验证运行手册。混合文档只保留未完成内容。不得仅通过提交推送永久保存任务流水账，也不得建立 `archive`、`history`、`completed` 等目录继续堆积已完成任务文档。

## 9. 验证与完成声明

- 声称通过前必须在当前工作状态上重新运行能够证明该结论的命令；
- 历史测试结果只能称为历史基线，不能当作本轮结果；
- 单元测试不能代替高风险路径的真实动态验证；
- 验证失败时报告实际状态，不得以“应该可以”结束；
- 提交前检查范围、差异、未跟踪文件、凭据、生成物和用户改动；
- 未经明确要求，不提交、不推送、不创建 PR、不部署、不自动发送外部消息。
