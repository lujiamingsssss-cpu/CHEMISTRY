# Chemical Trade AI Copilot 阶段性交接

> 更新时间：2026-07-28
>
> 适用对象：完全没有历史上下文的新开发会话
>
> 当前结论：阶段一已完成并推送；下一会话只能从阶段二“可信 RAG 核心”开始。

## 1. 新会话开始时先做什么

按以下顺序执行，不要从头重新规划：

1. 完整阅读 `PROJECT_SPEC.md`；它是当前唯一权威开发基线。
2. 阅读本文件和 `TASK_STATUS.md`。
3. 在 `F:\外贸化工` 运行 `git status --short --branch`，确认 `main` 与 `origin/main` 同步且无用户未提交改动。
4. 不重新整理已完成的资料，不恢复 `_excluded` 内容，不进入 UI。
5. 为阶段二先建立固定检索案例，再写生成代码。

历史参考会话 `019fa403-480a-73f2-99ce-3d97a9275c9b` 已被本轮重新读取，但从现在起应优先使用更新后的 `PROJECT_SPEC.md`，不要用旧会话覆盖当前事实。

## 2. 我们已经做了什么

### 项目与 Git

- 本地项目：`F:\外贸化工`
- GitHub：`https://github.com/lujiamingsssss-cpu/CHEMISTRY`
- 分支：`main`
- 阶段一实现提交：`62e6c9a`
- 阶段一状态提交：`0e20327`
- 两次提交均已推送；当时本地 HEAD 与远程 `main` 一致。

### API

- 已安全读取 `G:\桌面\化工外贸api.md` 并验证 DeepSeek 鉴权。
- 验证时可用模型：`deepseek-v4-flash`、`deepseek-v4-pro`。
- 密钥未进入代码、文档或 Git。
- 阶段一代码没有 DeepSeek 生成调用。

### 资料整理

资料根目录：`G:\桌面\化工`。

完成的人工级资料操作：

- `Acclaim® 4200N` 文件夹已更正为 `Acclaim® 4220N`；
- 错误重复的 `Desmodur® I` 已移入 `_excluded`；
- D.E.R. 331 的 2001 MSDS 已隔离；
- 已替换为 2020 英文 SDS；
- 已新增 EPON Resin 8280 的完整 TDS/SDS；
- D.E.N. 438 有 2026 SDS，但公开独立 TDS 下载需要账号，因此已隔离，不伪装成完整产品组。

正式索引白名单只有：

1. `D.E.R. 331`
2. `EPON Resin 8280`

正式资料共 4 份 PDF、36 个非空物理页。

### 已实现代码

- `materials.py`：固定白名单、根目录逃逸防护、TDS/SDS 成对校验；
- `pdf_pages.py`：PyMuPDF 按物理页提取；
- `embeddings.py`：本地 `multilingual-e5-small`，归一化向量；
- `retrieval.py`：页内分块、Chroma 持久化、文件名和页码元数据；
- `cli.py`：`ingest`、`query`；
- 自动化测试：资料、页码、分块、检索和 CLI 白名单。

运行方式：

```powershell
.\.venv\Scripts\python.exe -m chemical_trade_copilot ingest `
  --materials-root "G:\桌面\化工" `
  --database .chroma

.\.venv\Scripts\python.exe -m chemical_trade_copilot query `
  "EPON Resin 8280 heat deflection temperature MPDA cure schedule" `
  --database .chroma `
  --limit 1
```

阶段一最终验证：

- `9 passed in 5.80s`；
- `pip check` 无损坏依赖；
- 4 文档、36 页成功建库；
- 上述询盘首位命中 EPON 8280 TDS 第 3 页。

## 3. 当前卡在哪里

没有环境或权限阻塞。当前是**有意停在阶段边界**，不是开发失败。

真正尚未解决的问题是阶段二的可信度：

- 宽泛中文询盘对英文 TDS 的跨语言召回不稳定；
- top-k 检索可能漏掉同一产品的关键性能页；
- PDF 表格切块后可能把参数与条件拆开；
- 生成模型会在证据不全时把“没检索到”误写成“资料不存在”。

在这些问题有固定测试之前，不得继续生成产品结论或邮件。

## 4. 下一步计划

下一会话执行阶段二，且只执行阶段二。

### 第一步：建立最小 golden cases

先写测试数据，至少覆盖：

| 询盘意图 | 目标产品 | 目标文件 | 目标物理页 |
|---|---|---|---:|
| High solids coatings | EPON Resin 8280 | TDS - Hexion EPON Resin 8280 - Rev 2016.pdf | 1 |
| MPDA + HDT + cure schedule | EPON Resin 8280 | 同上 | 3 |
| Industrial/protective coating applications | D.E.R. 331 | TDS - DOW D.E.R. 331 - 2009.pdf | 1 |
| Formulated-system HDT | D.E.R. 331 | 同上 | 2 |
| 资料无法支持的要求 | 无 | 无 | 必须证据不足 |

先记录当前方案 Hit@K，不要先换模型。

### 第二步：只修真实失败

优先顺序：

1. 调整 top-k 或过滤低信息页眉块；
2. 必要时加入一层简单、可测试的查询改写；
3. 只有当前 E5 确认失败才比较更强多语言 Embedding；
4. 混合检索、RRF、重排器是最后选项。

每次只改一个变量并重跑 golden cases。

### 第三步：接入 DeepSeek 结构化输出

只有检索证据完整后再接生成模型。输出包含：

- 中文需求分析；
- 推荐产品或明确拒绝推荐；
- 推荐理由；
- 参数、单位、固化剂、配比、固化制度；
- 准确文件名和 PDF 物理页码；
- 资料缺口和追问项。

阶段二先不做 Streamlit，也不要做 UI。

### 第四步：验证并停止

- 自动化测试；
- 真实 PDF + 真实 DeepSeek 动态验证；
- 人工可读的证据抽查；
- 更新 `PROJECT_SPEC.md`、`TASK_STATUS.md`；
- 提交并推送；
- 立即停止，不进入阶段三。

## 5. 我们踩过的坑：绝对不要再踩

### 坑 1：把阶段一做成阶段二

本轮一度加入了中文查询改写、产品推荐和英文邮件生成。重新核对权威基线后确认越界，相关代码已撤回。

**禁止复发：** 新会话不要直接恢复或重写这些功能。阶段二必须先有 golden cases。

### 坑 2：检索没召回，就断言文档里没有

一次真实生成中，模型错误声称 EPON 8280 没有固化耐热数据；事实上 TDS 第 3 页明确有不同固化体系下的 131/156/131 °C HDT。原因只是 top-k 没把第 3 页送入上下文。

**禁止复发：** “top-k 没出现”只能表述为“当前检索证据不足”，不能表述为“原始资料不存在”。涉及缺失结论时必须全资料核验。

### 坑 3：脱离固化条件引用温度

EPON 8280 第 3 页的 HDT 属于不同固化体系；156 °C 与 MPDA 和对应固化制度绑定。D.E.R. 331 的 HDT 同样取决于具体配方。

**禁止复发：** 不得写成“树脂耐温 156 °C”。必须同时引用固化剂、配比、固化制度和页码。

### 坑 4：把 SDS 温度当使用温度

闪点、分解温度、储存温度和操作温度不是涂层连续使用温度。

**禁止复发：** SDS 只用于安全、运输、操作和法规证据，不替代 TDS 的应用性能证据。

### 坑 5：整页直接做向量

E5 有上下文长度限制。整页文本过长时，页尾关键参数会被截断。现已改为页内分块并保留物理页码。

**禁止复发：** 不要退回整页单向量；也不要为了分块丢失原始页码。

### 坑 6：低信息页眉/页脚产生虚高相似度

PDF 中存在只有产品名、章节标题或页码的短片段，可能干扰小型向量检索。

**处理原则：** 先用 golden cases证明它造成失败，再加最小过滤规则；不要预先建设复杂清洗流水线。

### 坑 7：查询改写不是天然可靠

单条中文改写曾把“耐热”泛化为 TGA/DSC，漏掉 TDS 实际使用的 HDT、cured-state properties、curing agent 和 cure schedule。两条查询虽能召回，但已经进入阶段二范围。

**禁止复发：** 查询改写必须用固定案例验证，不要仅凭一条漂亮输出认为可靠。

### 坑 8：同时堆多种检索增强

曾调研 BGE-M3、Haystack、LangChain MultiQuery、BM25、RRF、RAG-Fusion 和重排器。它们都是成熟方案，但当前只有两款产品。

**禁止复发：** 不要因为方案成熟就全部引入。复杂度必须由测量到的失败驱动。

### 坑 9：CLI 绕过白名单

独立审查发现早期 `--product` 可索引任意目录，甚至可能逃逸资料根目录。现已删除参数，并在资料层强制白名单与路径包含校验。

**禁止复发：** 不要重新开放任意产品或任意 PDF；第一版明确不支持在线导入。

### 坑 10：先删除旧索引再生成新向量

早期实现先清空 Chroma，再计算 Embedding；模型失败会丢失可用索引。现已改为先计算向量，再替换旧记录。

**禁止复发：** 保持当前顺序。

### 坑 11：把资料年份和管辖地区藏起来

D.E.R. 331 SDS 是加拿大英文版本；两份 TDS 年代较早。

**禁止复发：** Demo 必须如实展示文件年代、地区和局限，不能宣传为最新全球结论。

## 6. 不要复杂化的硬规则

- Demo 只有两款正式产品；优先把五个固定案例做准。
- 不要重写成熟库已有能力。
- 不要引入 Agent、工作流编排、知识图谱或生产数据库。
- 不要在没有对比指标时更换 Embedding。
- 不要在没有检索证据时让 LLM 自由回答。
- 不要在阶段二做 UI。
- 不要在阶段三 UI 获批前写正式界面。
- 不要创建更多管理文档。
- 不要自动进入下一阶段。

## 7. 人工检查点

- 资料配对：用户已授权本轮代办并已完成，不再重复请求。
- UI：阶段三必须先给用户线框图或截图审批。
- 推荐、参数、页码、邮件：阶段四由用户人工抽查。
- GitHub/账号：只有真实授权阻塞时才请求用户操作。

## 8. 交接完成标准

新会话如果只记住五件事：

1. 阶段一已经完成，不要重做。
2. 下一步先写 golden retrieval cases。
3. 检索没召回不等于资料不存在。
4. 技术参数必须和条件、文件名、物理页码绑定。
5. 这是两款产品的 Demo，不要把它做成生产平台。
