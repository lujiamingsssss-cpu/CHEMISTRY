# Chemical Trade AI Copilot 阶段性交接

> 更新时间：2026-07-28
>
> 适用对象：完全没有历史上下文的新开发会话
>
> 当前结论：阶段一、阶段二、阶段三均已完成、验证并推送。阶段三采用用户批准的 Editorial Brief / Mineral Ink 英文单页；阶段三功能最终提交为 `96126ac`，本交接更新后 `main` 与 `origin/main` 应同步在 Git 最新 HEAD。项目有意停止在阶段四之前，下一会话不得重做前三阶段。

## 1. 权威顺序与新会话启动步骤

文档权威顺序：

1. `PROJECT_SPEC.md`：当前唯一权威开发基线；
2. `TASK_STATUS.md`：最新验证与 Git 状态；
3. `HANDOFF.md`：帮助零上下文会话快速接手，不能覆盖前两份文件。

新会话必须先执行：

1. 完整阅读以上三份文件；
2. 在 `F:\外贸化工` 运行 `git status --short --branch`；
3. 运行 `git log -5 --oneline`，确认 `96126ac` 及阶段三提交仍在历史中；
4. 如要改代码，先运行当前测试作为基线；
5. 不整理或恢复 `_excluded`，不重建前三阶段，不擅自进入阶段四。

旧任务和历史对话只能作为背景，不能覆盖 `PROJECT_SPEC.md`。尤其不要按旧交接重新建立 golden cases；它们已经完成。

## 2. 项目现在是什么

这是一个面向私人业务的“化工外贸询盘证据工作台”Demo：把中英文客户询盘拆成技术、合规、商务和物流要求，用企业自己的 TDS/SDS 给出可追溯的产品判断、证据缺口和下一步动作。

它不是：

- 公开化工商城；
- 供应商撮合或全市场比价平台；
- 通用 PDF 聊天工具；
- CRM、ERP 或自动邮件系统；
- 可处理任意产品和任意上传文件的生产平台。

竞品调研后的结论没有改变技术主线：同行已经在做海量目录、样品/询价、供应商发现和替代品匹配。本项目的差异化价值是对企业自有小型资料做可信、可核查的询盘处理。不要为了“跟上时代”复制市场平台。

## 3. 固定位置、安全边界与资料边界

- 项目：`F:\外贸化工`
- GitHub：`https://github.com/lujiamingsssss-cpu/CHEMISTRY`
- 分支：`main`
- 资料：`G:\桌面\化工`
- DeepSeek 本机配置：`G:\桌面\化工外贸api.md`
- 阶段二核心实现提交：`4acdf2c`
- 阶段三最终实现提交：`96126ac`
- 当前远端：`origin/main`，已与本地 `main` 同步；本交接若产生新文档提交，则以 Git 最新 HEAD 为准。

API 密钥只能在本机读取后放入当前进程环境变量。禁止输出、记录、提交或复制到 `.env`、源码、测试、Markdown、日志和对话。

正式白名单固定为：

1. `D.E.R. 331`
2. `EPON Resin 8280`

正式资料共 4 份 PDF、36 个非空物理页：

- D.E.R. 331：2009 TDS 5 页；2020 加拿大英文 SDS 13 页；
- EPON Resin 8280：2005 重新发布、页脚修订 2016 的 TDS 5 页；2022 美国英文 SDS 13 页。

`G:\桌面\化工\_excluded` 不得索引，其中包括缺少独立 TDS 的 D.E.N. 438、D.E.R. 331 的 2001 旧 MSDS，以及错误重复的 Desmodur I 目录。

## 4. 已完成：阶段一

阶段一“资料与最小可行性验证”已完成，不得重做：

- 盘点、哈希、文本提取和页面渲染检查；
- 纠正错误目录、隔离重复/旧版/不完整资料；
- 固定两款产品白名单，并校验 TDS/SDS 成对；
- PyMuPDF 按物理页提取；
- LangChain 页内分块且保留物理页码；
- `intfloat/multilingual-e5-small` 本地归一化向量；
- Chroma 本地持久化索引；
- 根目录逃逸防护；
- `ingest`、`query` CLI；
- 先计算新向量、成功后再替换旧索引。

基础链路：

```text
固定白名单
  → 物理页提取
  → 页内分块
  → multilingual-e5-small
  → Chroma
  → 命中片段 + 产品 + 文档类型 + 文件名 + 物理页码
```

## 5. 已完成：阶段二

阶段二“可信 RAG 核心”已完成并推送，未做 UI。

### 5.1 Golden retrieval cases

已建立 5 条固定案例：

| 案例 | 预期证据 |
|---|---|
| EPON 8280 high-solids coatings | EPON TDS 第 1 页 |
| EPON 8280 + MPDA + HDT | EPON TDS 第 3 页 |
| D.E.R. 331 汽车、船舶和防护涂料 | D.E.R. TDS 第 1 页 |
| D.E.R. 331 配方体系 HDT | D.E.R. TDS 第 2 页 |
| 200°C 连续使用 + 食品接触认证 + 长期寿命 | 必须证据不足 |

真实外贸措辞下：

- 原始 E5 + Chroma：Hit@1/3/5 = `0.25/0.25/0.25`；
- 简洁查询规划 + TDS/SDS 类型过滤后：Hit@1/3/5 = `0.75/1.00/1.00`。

只做了一个最小增强：DeepSeek 把询盘中的技术检索意图与商务噪声分开，并选择 TDS/SDS 类型。没有更换 Embedding，没有引入 BM25、RRF、重排器或 RAG 框架。

### 5.2 结构化询盘分析

`analyze` CLI 已实现：

- 需求拆分：技术、合规、商务、物流；
- 推荐或明确证据不足；
- 推荐理由；
- 关键参数和引用；
- 证据缺口、追问和下一步动作；
- DeepSeek JSON 输出为空时重试一次；
- 模型输出两次不能通过本地校验时，确定性降级为证据不足。

### 5.3 本地证据门禁

当前门禁不是单靠提示词：

- 所有引用必须存在于本次证据集合；
- `supported` 必须有引用，当前只允许技术项为 `supported`；
- 产品推荐必须有同产品的技术引用；
- 合规、库存、价格、MOQ、交期和物流不能由当前 TDS/SDS 自动确认；
- 关键参数只能来自人工核验事实集；
- 温度要求必须匹配同引用、同数值、同物性语义的核验事实；
- 温度值只允许出现在已核验的 `key_parameters`，不能在摘要、理由、缺口或追问中自由复述；
- HDT 不能当作连续使用温度，即使数值完全相同；
- 普通 `query` 不输出内部完整页文本 `page_text`。

目前唯一完成完整条件核验的原子事实是：

```text
产品：EPON Resin 8280
物性：Heat Deflection Temperature
值：156°C
固化剂：Metaphenylenediamine (MPDA)
配比：EPON Resin 8280 100 pbw : MPDA 14.4 pbw
固化制度：2 h/80°C + 2 h/150°C
测试方法：ASTM D648
来源：TDS - Hexion EPON Resin 8280 - Rev 2016.pdf，物理第 3 页
```

D.E.R. 331 的跨列表格没有完整可靠地绑定所有条件，因此没有加入事实白名单。接口予以保留，未来可以在人工核验后扩充事实集；不要为了“功能更多”绕过白名单。

### 5.4 已验证结果

- 全量自动化测试：`56 passed`；
- `pip check`：无损坏依赖；
- wheel：构建成功；
- 规划检索 4 条可回答案例 Hit@3 = `1.00`；
- MPDA 正例可以输出 EPON Resin 8280 和完整条件；
- 200°C 连续使用/食品接触认证负例无推荐、无技术参数；
- DeepSeek 偶尔会输出不合规 JSON 或重复温度，系统会重试或安全降级。这是保守行为，不得通过放宽证据规则“提高成功率”。

## 6. 已完成：阶段三 Web 界面

阶段三没有重写阶段二。`workflow.py` 只把原有“规划 → 类型过滤检索 → 完整物理页合并 → 本地证据校验”整理为 CLI 与页面共用的薄调用层。

已完成的真实界面状态：

1. 英文询盘入口：接受中英文询盘，不提供任意文件上传；
2. 证据支持结果：展示产品、四类状态、开放项、下一步、条件绑定参数、报价前待确认项和文档包状态；
3. 证据不足结果：不显示产品或未核验数值，只展示与本次需求类别对应的缺口和追问；
4. 来源查看器：只渲染白名单 PDF 的准确物理页，点击打开，支持 `50%`–`250%` 缩放、复位和点击切换；
5. 英文邮件：由已通过门禁的结构化结果确定性生成，可编辑、可复制、不连接邮箱、不自动发送。

UI 约束：

- 风格是 Editorial Brief / Mineral Ink，不做多栏工作台、密集仪表盘或商城；
- 数字使用直立字形和 tabular numerals，不使用倾斜显示；
- 真实来源必须同时显示产品、准确文件名、物理页码、日期/修订和管辖信息；放大层打开后这些信息仍需可见；
- 普通应用适用性正例只能使用保守的“文档范围内有技术回复证据”措辞；只有存在已核验的固化剂、配比、固化制度和测试方法时，邮件才可写“specified cured-system test result”；
- 四类状态、开放项、客户追问和下一步必须来自当前 `requirements` 与 `next_action`，不得用固定模板伪装成分析结果。

阶段三验证：

- 合并到 `main` 后全量自动化测试：`56 passed`；
- `pip check`：`No broken requirements found`；
- wheel：构建成功；
- 真实浏览器正例：EPON Resin 8280 + MPDA + HDT 输出完整条件、ASTM D648、TDS 物理第 3 页；
- 真实浏览器负例：200°C 连续使用 + 食品接触认证 + 长期寿命返回证据不足，无产品、无技术数值；
- 查看器缩放、复位、点击切换和邮件复制已真实验证；
- 阶段复杂度审计与独立代码复审完成，没有遗留 Critical 或 Important 问题。

## 7. 当前卡在哪里

当前没有环境、资料、权限、测试或 Git 阻塞。阶段三已经完成，项目是**按阶段停止在阶段四之前**，不是开发失败。

阶段三已完成：

- 用户批准 Editorial Brief / Mineral Ink 单页方向；
- 英文 Streamlit 单页直接调用阶段二可信分析链路；
- 支持/证据不足两种结果状态、四类决策状态、条件绑定参数、报价准备项和文档包状态；
- 白名单真实 PDF 页可点击查看，支持 `50%` 至 `250%` 缩放、复位和点击切换；
- 英文邮件可编辑、可复制但不自动发送；
- 正向、负向真实浏览器案例和自动化测试均通过。

尚未完成且本轮不得自动进入的是阶段四演示验收、用户人工抽查和最终截图或录屏。

非阻塞限制：

- 白名单只有两款产品，不能代表市场级选型；
- 两份 TDS 年代较早；
- 当前只有一条完整人工核验参数事实；
- DeepSeek 输出存在非确定性，失败时会安全降级；
- 商务、库存、物流和认证仍需人工或未来可信数据源。

## 8. 下一步计划：等待用户启动阶段四

只有用户明确要求继续时，才按 `PROJECT_SPEC.md` 进入阶段四。建议严格按以下顺序：

1. 完整读取 `PROJECT_SPEC.md`、`TASK_STATUS.md`、`HANDOFF.md`，确认 `main` 和远端状态；
2. 运行当前 56 项测试作为基线，不修改检索、事实门禁或 UI 信息结构；
3. 启动本地 Streamlit，固定演示三个案例：普通应用适用性正例、MPDA/HDT 条件绑定正例、200°C/食品接触负例；
4. 让用户人工抽查产品、条件、数值、文件名、物理页码、日期/管辖和英文邮件；
5. 只修复验收中可复现的正确性或展示问题；不要借演示验收增加平台能力；
6. 验收稳定后制作最终截图或录屏；
7. 部署必须另行获得用户决定，不是阶段四默认动作；
8. 更新现有三份管理文档、测试、提交、推送并停止。

阶段三复杂度审计已经确认：新增运行依赖只有 Streamlit；没有任意上传、账号、后台、生产数据库、供应商平台、图片生成、自动发信或新增检索框架。实时价格、库存、MOQ、运费、交期、目标市场认证、更多产品和生产部署均缺少当前可信数据或授权，禁止为了页面完整而硬开发。

英文邮件继续只能依据已经通过本地门禁的分析生成，必须可编辑，不能自动发送，不能补写未确认的价格、库存、MOQ、交期或认证。

## 9. 绝对不要再踩的坑

### 9.1 不要重做已完成阶段

阶段一、阶段二和阶段三已经完成。不要重新整理资料、重新选择向量库、重新设计 golden cases、重做 UI，或把已完成工作描述成待办。

### 9.2 不要因竞品调研把 Demo 变成市场平台

商城、供应商网络、实时价格、撮合和海量产品目录是别人的产品路径，不是当前项目边界。

### 9.3 不要把问题复杂化

当前两款产品已经用 E5 + Chroma + 一次查询规划达到 Hit@3 = 1.00。没有固定失败案例时，禁止增加 BGE-M3、BM25、RRF、多查询、重排器、Agent、工作流编排、知识图谱或生产数据库。

### 9.4 不要把 top-k 未命中写成资料不存在

top-k 只代表排序结果。当前实现会在排序后把所选文档类型的完整页集合用于核验。没有完整核验时只能说“当前检索证据不足”。

### 9.5 不要让商务噪声污染技术检索

MOQ、CFR、库存、目的港和交期曾显著拉低召回。查询规划只提取可在 TDS/SDS 搜索的技术/安全意图，不直接回答询盘。

### 9.6 不要退回整页单向量，也不要只给模型命中小块

整页可能超过 E5 上下文，页尾参数会丢失；只给模型小块又可能拆散表格条件。当前设计是“小块排序、完整物理页核验、准确页码溯源”。

### 9.7 不要自行配对 PDF 表格列

模型曾把 24 h/23°C 与 MPDA 列错误绑定。EPON 的 156°C 事实由人工核验并规范化；D.E.R. 跨列表格未核验，因此不自动输出数值。

### 9.8 不要把 HDT/Tg 当树脂本体耐温或连续使用温度

固化体系性能必须与固化剂、配比、固化制度和测试方法绑定。HDT 与连续使用温度是不同物性，同为 156°C 也不能互换。

### 9.9 真实引用不等于主张真实

模型曾用真实 EPON 第 3 页包装虚构黏度、库存和认证。引用身份校验不够，所以关键参数采用人工事实白名单，非技术项不得自动标记为 supported。

### 9.10 不要在自由叙述中重复温度

模型会把合法 HDT 在摘要中改写成“可长期连续使用”。温度只放在经过核验的结构化参数中，不为了文案好看重复到摘要和推荐理由。

### 9.11 不要只靠反复修改提示词

空 JSON、截断、错误表格配对和同义词绕过都发生过。高风险规则必须由本地校验和回归测试保证；提示词只负责引导。

### 9.12 不要把 SDS 温度当应用性能

闪点、分解温度、储存温度和操作温度不能替代涂层连续使用温度。SDS 只用于安全、运输、操作和管辖信息。

### 9.13 不要泄露内部完整页文本

`page_text` 用于内部证据核验，普通 `query` JSON 只输出命中块和溯源元数据。

### 9.14 不要重新开放任意产品或任意上传

CLI 不允许 `--product` 覆盖。资料发现层和 CLI 层都必须保持白名单及根目录校验。

### 9.15 不要先清空索引再计算向量

保持“先成功计算新向量，再替换旧记录”，避免模型或 Embedding 失败后丢失可用索引。

### 9.16 不要为了精简删除正在使用或可验证扩展的接口

`doc_types` 过滤、完整页读取、golden evaluator、结构化 schema 和事实集扩展点都有当前用途或清晰升级路径，应保留。边界控制的含义是“不启用、不预建空平台”，不是把未来可能使用的可靠接口全部删除。

### 9.17 不要隐瞒资料年代、地区或模型降级

旧 TDS、加拿大/美国 SDS 和模型 fail-closed 都必须如实展示。安全降级不是成功推荐，也不是系统故障。

### 9.18 不要创建更多管理文档

只维护 `PROJECT_SPEC.md`、`TASK_STATUS.md` 和本文件。除非用户再次明确要求，不创建额外计划、调研、决策、日志或交接 Markdown。

### 9.19 不要让邮件模板超出实际证据类型

阶段三复审曾发现：只证明“high-solids coatings 应用”的支持结果也会套用“固化体系试验已支持”的邮件句式。现已修复。以后必须按真实核验事实选择措辞；有真实引用不代表可以补写另一类技术主张。

### 9.20 不要用固定 UI 文案覆盖真实分析结果

四类状态、证据缺口、追问和下一步曾被固定模板统一展示，导致不同询盘看起来得到相同结论。现已改为按结构化类别和 `next_action` 保守映射。不要为了页面整齐恢复成“一套话术走天下”。

### 9.21 不要让放大层遮住来源身份

来源缩略图和放大层都必须持续显示产品、文件名、物理页码、日期/修订和管辖信息。元数据只允许来自四份批准文件的固定可信映射，不能由模型自由生成。

### 9.22 不要用环境偶然性掩盖工作树导入错误

隔离工作树测试曾错误导入主工作区的旧 editable package，导致新模块在全量测试收集时缺失。`pyproject.toml` 已用 `pythonpath = ["src"]` 固定当前仓库源码优先。不要靠临时 `PYTHONPATH` 或重新安装来掩盖测试入口问题。

### 9.23 不要为无关文件监视告警安装大型依赖

Streamlit 文件监视器枚举 `transformers` 懒加载模块时曾触发未使用的 `torchvision` 导入告警。根因不是页面缺依赖，现已通过 `[server] fileWatcherType = "none"` 关闭开发热监视。不要安装 torchvision，也不要把该告警误判为模型或页面功能失败。

### 9.24 不要为模型延迟预建后台架构

真实两段 DeepSeek 调用可能需要约 1–3 分钟。当前 Demo 有加载状态并保持 fail-closed；没有固定验收案例证明必须异步化之前，禁止新增队列、任务系统、缓存服务、轮询 API 或第二套模型链路。

## 10. 常用验证命令

```powershell
cd "F:\外贸化工"

git status --short --branch

.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe -m pip check
.\.venv\Scripts\python.exe -m build --wheel

.\.venv\Scripts\python.exe -m streamlit run src\chemical_trade_copilot\streamlit_app.py

.\.venv\Scripts\python.exe -m chemical_trade_copilot query `
  "EPON Resin 8280 heat deflection temperature MPDA cure schedule" `
  --database .chroma `
  --limit 3
```

`analyze` 需要当前进程中的 `DEEPSEEK_API_KEY`。密钥从本机配置安全加载，不要把加载后的值打印到终端。

构建验证会生成 `dist`；不要把 wheel 当源码提交。`.chroma`、本地 PDF、模型缓存、`.venv`、`.env` 和浏览器日志也不得进入 Git。

## 11. 交接完成标准

新会话至少要记住：

1. `PROJECT_SPEC.md` 是唯一权威基线；
2. 阶段一、阶段二、阶段三都已完成，不要重做；
3. 当前没有阻塞，`main` 已推送，下一步只能由用户明确启动阶段四；
4. 这是两款产品的私人 Demo，不是商城或生产平台；
5. 不要复杂化，增强必须由固定失败案例驱动；
6. 检索没命中不等于资料不存在；
7. 真实引用不等于真实主张，关键参数必须经过事实门禁；
8. HDT/Tg 不能当连续使用温度；
9. 保留已有可靠接口，但不要预建未启用的平台能力；
10. 每阶段测试、动态验证、文档、提交和推送后立即停止。
