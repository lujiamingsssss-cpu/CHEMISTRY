# Chemical Trade AI Copilot 任务状态

## 当前阶段

阶段四：演示验收（已完成、验证并随本交接提交推送）。

阶段一、阶段二、阶段三保持完成状态，本轮没有重做。用户已批准阶段五“统一可扩展资料目录与安全索引重建”的设计，但尚未实现。

## 已完成并验证

- 已完整读取任务 `019fa420-c921-7581-81ce-9aefed995944`，并完整读取 `HANDOFF.md`、`PROJECT_SPEC.md`、`TASK_STATUS.md`。
- 已在 `F:\外贸化工` 初始化 Git `main` 分支，并配置远程 `https://github.com/lujiamingsssss-cpu/CHEMISTRY.git`。
- 已安全读取 `G:\桌面\化工外贸api.md` 并验证 DeepSeek 鉴权；密钥未写入项目。API 当前返回 `deepseek-v4-flash`、`deepseek-v4-pro`。
- 已盘点和渲染检查资料目录全部初始 PDF；均可提取文本，无扫描件阻塞。
- 已更正 `Acclaim® 4200N` 为 `Acclaim® 4220N`。
- 已将错误重复的 `Desmodur® I` 移到 `_excluded`。
- 已用 2020 英文 SDS 替换 D.E.R. 331 的 2001 MSDS；旧文件保留在 `_excluded`。
- 已新增 EPON Resin 8280 的完整 TDS/SDS 组。
- D.E.N. 438 只有 2026 SDS，独立 TDS 需要账号，已放入 `_excluded`，不进入索引。
- 正式白名单现有 4 份 PDF、36 个非空物理页：
  - D.E.R. 331：TDS 5 页、SDS 13 页；
  - EPON Resin 8280：TDS 5 页、SDS 13 页。
- 已实现显式白名单发现、TDS/SDS 成对校验、PyMuPDF 页级抽取、页内分块、本地 E5、Chroma 持久化及 ingest/query CLI。
- 白名单同时在资料发现层和 CLI 层强制执行；CLI 不提供产品覆盖参数，并拒绝根目录外或未审批产品。
- 已建立 5 条 golden cases：4 条可回答案例精确到产品、文件、物理页，1 条要求证据不足。
- 真实外贸措辞下原始 E5 + Chroma 基线 Hit@1/3/5 为 `0.25/0.25/0.25`。
- 仅增加 DeepSeek 简洁查询规划和 TDS/SDS 类型过滤；继续使用原 E5 + Chroma，Hit@1/3/5 提升为 `0.75/1.00/1.00`。
- 已实现结构化询盘分析：技术、合规、商务、物流四类需求，产品建议、证据缺口、追问和下一步动作。
- 已实现证据门禁：引用必须命中本次证据；所有 `supported` 项必须有引用且当前只允许技术项；支持推荐必须有同产品的技术引用；关键参数必须进入人工核验事实集，温度型技术项还必须匹配同引用、同数值和同物性语义；温度数值只允许出现在已核验关键参数中，需求项或自由叙述均不得把 HDT 改写成连续使用温度；无依据合规/商务/物流结论、游离温度和矛盾叙述会拒绝。
- DeepSeek 两次输出仍未通过本地校验时，系统确定性降级为证据不足，不采纳产品结论或技术数值。
- 普通 `query` 继续只返回命中块和溯源信息，内部完整页文本不对外暴露。
- 竞品方向复核后，产品定位收窄为“私人化工外贸询盘证据工作台”，不转向公开商城、供应商撮合或通用 PDF 问答。
- 用户已批准 Editorial Brief / Mineral Ink 单页信息结构和高级配色；`docs/UI_STYLE_GUIDE.md` 作为本轮明确授权的唯一新增设计说明约束真实页与展示方向一致。
- 已增加英文 Streamlit 单页，直接复用阶段二的规划、检索、完整物理页证据校验和安全降级，不展示未经本地门禁的模型自由叙述。
- 已实现四类决策状态、条件绑定关键参数、准确来源、报价前待确认项、文档包状态，以及可编辑、可复制但不自动发送的英文邮件。
- 已实现白名单真实 PDF 页点击查看器，支持 `50%` 至 `250%` 缩放、复位和点击切换；产品、文件名、页码及根目录均受控。
- 已完成阶段复杂度审计：新增运行依赖只有 Streamlit；没有上传、账号、后台、生产数据库、供应商平台、图片生成、自动发信或新增检索框架。价格、库存、MOQ、运费、交期和认证只作为人工待确认项。
- 已从既有五条 golden cases 中冻结三个阶段四演示案例，没有重建阶段二基准：普通应用正例、MPDA/HDT 条件绑定正例、200°C/食品接触认证负例。
- 普通应用正例命中 EPON Resin 8280 TDS 物理第 1 页；邮件只使用“文档范围内可技术回复”的保守措辞。
- MPDA/HDT 正例命中 EPON Resin 8280 TDS 物理第 3 页，完整展示 MPDA、100:14.4、2 h/80°C + 2 h/150°C、156°C HDT 和 ASTM D648，并明确不是连续使用温度。
- 负例没有推荐产品、没有输出技术数值或认证；验收发现并修复了 safe fallback 丢失食品接触认证类别的问题。
- safe fallback 现在保留原询盘中明确出现的 compliance、commercial 和 logistics 类别，同时保持产品、引用和技术数值全部 fail-closed。
- 分类匹配使用词/短语边界，区分法规 `21 CFR` 与贸易术语 CFR，并覆盖 compliance、常见 Incoterms 和 lead time；不会因 `reach`、`technical support`、`specification` 子串误报。
- 阶段四修复已按 TDD 完成，经过两轮独立代码复审；最终没有遗留 Critical 或 Important 问题。
- 已生成并视觉核对三张最终演示截图：`docs/demo/phase4-01-high-solids-supported.png`、`phase4-02-mpda-hdt-supported.png`、`phase4-03-insufficient-evidence.png`。

## 剩余收口事项

阶段四没有剩余开发或验收事项。阶段五设计已批准但尚未实现；当前按会话边界停止，不得把设计描述为已交付。

## 人工检查点

- 产品和 TDS/SDS 对应关系原本是阶段一人工停止点；用户已明确授权本会话代为完成资料操作。已通过产品名、文档类型、页数、哈希、文本提取和页面渲染复核完成。
- 阶段三 UI 方向已经用户审批，实施结果按批准方向完成。
- 阶段四的推荐、参数、页码、来源元数据和英文邮件抽查已经通过。
- 下一人工检查点是阶段五新资料清单首次启用前的产品、文件版本、日期/管辖和 golden cases 审批。

## 最近状态

- 自动化测试：2026-07-28，阶段四修复分支全量运行 `62 passed`；合并和文档更新后将再次运行。
- 依赖检查：2026-07-28，`pip check` 返回 `No broken requirements found`；wheel 构建成功。
- 真实动态验证：2026-07-28，普通应用正例、MPDA/HDT 正例和 200°C/食品接触负例均通过；负例修复后 Compliance 显示 `Confirmation required`，开放项和追问包含目标市场法规，同时仍无产品、无技术数值。
- 运行日志：关闭 Streamlit 源码热监视后，不再因枚举 `transformers` 懒加载模块触发无关 `torchvision` 告警；健康检查和页面加载正常。
- Git：阶段四修复与截图已快进合并到本地 `main`，提交为 `e688091`；本次交接文档提交并推送后以 Git 最新 HEAD 为准。阶段四临时工作树将在最终验证后清理。

## 当前风险与非阻塞问题

- 当前白名单只有两款产品，不能代表市场级选型。
- 两份 TDS 年代较早；输出必须持续披露日期和适用范围限制。
- 目前只有 EPON Resin 8280 第 3 页的一条 HDT 原子事实完成完整条件核验；D.E.R. 331 的跨列表格不做自动数值配对。
- 商务价格、库存、MOQ、交期和目标市场认证仍需人工或未来系统输入。
- 当前资料发现仍固定两款产品，并要求每款恰好一份 TDS 与一份 SDS；受控扩展能力尚未实现。
- 当前 `PageIndex.replace` 是正式集合内全量替换，不是带独立临时索引、原子切换与回滚的最终扩展流程。
- 当前 PDF 查看器来源元数据仍基于固定批准映射；阶段五必须与统一资料清单合并为单一事实来源。
- 用户已明确允许阶段五在客观需要时编写最小浏览器插件，用于从批准的公开官方页面下载选定资料并做真实扩展验收；插件尚未实现，也不是默认必需组件。

## 下一轮继续位置

阶段四已正式完成，阶段五是独立后续能力，顺序没有冲突。下一会话应先读取三份管理文档并运行 62 项测试，再按 `PROJECT_SPEC.md` 建设统一批准清单、资料预检、临时索引构建、golden 回归、原子切换和回滚。

阶段五必须用至少一组真实新增官方 TDS/SDS 验证扩展能力。先尝试普通浏览器或直接下载；只有公开页面确实依赖浏览器交互时，才实现 `tools/material_acquisition_extension/` 下的最小 Chrome/Edge Manifest V3 插件。插件只下载人工选定、批准域名中的文件，输出到隔离待审区，不自动批准或索引，不绕过账号、验证码、付费墙、许可或访问控制。新资料通过人工对应复核、哈希与元数据预检后，才能进入统一清单、临时索引和新增 golden case。不得重做前四阶段，也不得扩展为任意上传、公开目录、无人监督爬虫、第二向量库或复杂检索平台。

阶段五在确定资料获取方式、接通统一清单、实现索引切换和完成真实扩展验收后分别执行复杂度自省。需求解释以参考会话、权威基线、现有代码和真实产品行为为证据；能用普通下载、一个清单和一次受控全量重建解决时就停止加层。不得因原始描述模糊而擅自扩大范围。
