# Chemical Trade AI Copilot 稳定产品约束

本文件只记录仓库内既有 Chemical Trade AI Copilot 的长期产品与安全边界，不记录阶段状态、验证流水、Git 状态或下一任务。仓库级工程权威以根目录 `AGENTS.md` 为准；公开展示前台的产品方向以 `docs/adr/0001-public-showcase-product-transition.md` 和 `docs/SHOWCASE_UI_STYLE_GUIDE.md` 为准。

## 1. 产品定位

Chemical Trade AI Copilot 是基于企业批准 TDS/SDS 的私人化工外贸询盘证据工作台。它把中英文询盘拆为技术、合规、商务和物流要求，给出有来源约束的产品判断、证据缺口、追问和可编辑英文回复草稿，但不自动发送。

它不是公开商城、供应商撮合平台、通用 PDF 问答、CRM、ERP 或生产系统。

## 2. 范围与非目标

当前受控资料只覆盖批准清单 `materials_catalog.json` 中启用的工业涂料用环氧树脂产品。现有三款产品为：

- D.E.R. 331；
- EPON Resin 8280；
- BAER XP9500。

不在当前范围内：

- 任意 PDF 上传、无人监督抓取或登录/付费内容绕过；
- 实时价格、库存、MOQ、运费、交期或供应商网络；
- 自动发送邮件、账号权限、CRM/ERP 集成和生产数据库；
- 未经证据支持的产品推荐、认证结论或工程参数；
- 为小型资料库预建第二检索链路、后台任务平台或 Agent 框架。

## 3. 资料与安全边界

- 正式资料根目录位于仓库外部，由本机受控配置指定；PDF、索引、模型缓存、凭据和私有客户资料不得进入 Git。
- API 密钥只允许在本机安全读取后放入进程环境变量，不得进入源码、测试、Markdown、日志或对话输出。
- 入库、索引、查询、状态检查和 PDF 查看器必须读取同一份 `materials_catalog.json`。
- 每个启用产品必须恰好有一份当前 TDS 和一份当前 SDS；路径、哈希、文件身份、版本、地区和启用状态必须通过预检。
- 清单外、根目录外、哈希不符、身份不明或未获人工批准的资料不得进入正式索引。

## 4. 检索与证据门禁

- 当前检索主链保持为单一 multilingual E5 + Chroma；文档和查询分别使用 `passage:`、`query:` 前缀并归一化向量。
- 简洁查询规划和 TDS/SDS 类型过滤是当前允许的检索增强。只有固定失败案例证明不足时，才依次考虑调整 top-k、清理低信息块、简单改写、更强多语言 Embedding，最后才考虑混合检索或重排。
- 普通查询只返回命中片段及产品、文件名、文档类型和物理页码；内部完整页文本不得对外暴露。
- “检索未命中”只能表述为当前检索证据不足，不能推断原始资料不存在。
- 模型输出必须通过本地确定性校验；两次输出仍不合格时安全降级为证据不足，不放宽门禁或盲目重试。

## 5. 化工专业规则

- 固化后 HDT/Tg 必须绑定固化剂、配比、固化制度、测试方法、文件和物理页码。
- 不得把某个固化体系的性能归因于未固化树脂本身。
- 不得把 SDS 的闪点、分解温度、储存温度或操作温度解释为连续使用温度。
- 表格列关系不明确时不得自行配对数值。
- 产品名称、参数、单位、适用条件、来源文件和页码必须来自证据。
- 资料年代、开发状态、地区和适用限制必须披露。
- 合规、商务、物流和专业化工判断必须保留人工确认边界。

## 6. 用户界面边界

- 既有 Streamlit 页面遵循 `docs/UI_STYLE_GUIDE.md`。
- 页面展示结构化需求、推荐或证据不足状态、条件绑定参数、准确来源、报价前开放项和英文邮件草稿。
- PDF 查看器只能访问批准清单中的真实文件与物理页，不接受任意路径。
- 英文邮件只使用通过本地门禁的结构化内容，可编辑、可复制但不自动发送，也不补写未确认的价格、库存、MOQ、交期或认证。

## 7. 受控资料扩展

后续增加产品或资料必须遵循 `docs/MATERIAL_EXPANSION_RUNBOOK.md`：先隔离和人工审批，后更新清单；先写必要 golden case，后启用资料；在暂存位置全量重建并通过完整性与回归门禁后才切换正式索引；失败时保持旧索引可用并提供回滚。

新增资料可以参与检索，但 HDT、Tg、认证和其他高风险事实仍须单独人工核验后才能进入结构化支持输出。

## 8. 已知限制

- 三款产品只足以验证窄场景，不能代表全市场选型能力。
- 部分 TDS 年代较早或带开发状态限制，SDS 管辖地区不同。
- 当前只有少量高风险参数完成完整原子事实核验。
- 外部模型响应具有延迟和非确定性，本地证据门禁与安全降级不能省略。
