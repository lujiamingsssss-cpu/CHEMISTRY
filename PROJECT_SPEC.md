# Chemical Trade AI Copilot 项目开发基线

> **本开发基线更新于 2026-07-28，是当前唯一权威版本；此前与本基线冲突的计划、范围和技术假设全部失效。**

## 1. 项目定位

制作一个基于公开化工产品 TDS/SDS 的浏览器 Web Demo，证明 AI 可以减少外贸人员翻阅 PDF、核对参数和整理回复的时间。

第一版只覆盖：**工业涂料用环氧树脂**。

最终用户流程：

1. 输入中文或英文客户询盘；
2. 系统分析需求；
3. 检索真实 TDS/SDS；
4. 推荐有证据支持的真实产品；
5. 展示关键参数、适用条件、准确文件名和 PDF 物理页码；
6. 生成可编辑的英文外贸回复邮件。

项目是演示 Demo，不是生产平台。核心价值是“一次输入得到可核查的业务草稿”，但可信度优先于功能数量。

## 2. 任务边界

第一版不做：

- 模型训练或微调；
- 移动 App；
- 用户注册和权限系统；
- CRM、ERP 或邮件系统集成；
- 生产数据库；
- 自动发送邮件；
- 在线上传任意 PDF；
- 复杂后台或工作台；
- 真实客户敏感数据处理；
- 聚氨酯或涂料添加剂；
- 无证据的产品推荐；
- 为两款产品的小型 Demo 预先建设生产级混合检索平台。

**不要将问题复杂化。** 任何新依赖、检索层、代理框架、重排模型或数据处理流程，都必须由固定测试案例证明现有简单方案确实失败后才能引入。

## 3. 项目位置与安全边界

- 本地目录：`F:\外贸化工`
- GitHub：`https://github.com/lujiamingsssss-cpu/CHEMISTRY`
- 资料目录：`G:\桌面\化工`
- DeepSeek 配置：`G:\桌面\化工外贸api.md`

API 密钥只能在本机安全读取并放入进程环境变量。不得出现在对话、命令输出、日志、源代码、项目文档或 Git 历史中。

项目管理文档仅允许：

1. `PROJECT_SPEC.md`：当前权威开发基线；
2. `TASK_STATUS.md`：当前状态与验证记录；
3. `HANDOFF.md`：2026-07-28 阶段性交接快照，是用户明确要求的例外。

除非用户再次明确要求，不得继续创建计划、调研、决策、日志或交接类管理文档。

## 4. 当前资料边界

当前正式白名单固定为两组产品：

- `D.E.R. 331`
  - TDS：2009，5 页；
  - SDS：2020，加拿大英文，13 页。
- `EPON Resin 8280`
  - TDS：2005 重新发布，页脚修订 2016，5 页；
  - SDS：2022，美国英文，13 页。

四份正式 PDF 共提取出 36 个非空物理页。

`G:\桌面\化工\_excluded` 中的内容不得索引，包括：

- 缺少独立 TDS 的 D.E.N. 438；
- D.E.R. 331 的 2001 旧 MSDS；
- 与 Desmodur HL BA 重复的错误 `Desmodur I` 目录。

资料限制：两款产品足以展示窄场景，不代表全市场选型。两份 TDS 年代较早，输出必须披露资料日期和证据限制。

## 5. 已完成的阶段一

阶段一“资料与最小可行性验证”已完成并推送。

已交付：

- Git 与 Python 项目初始化；
- DeepSeek API 安全验证；
- 全部初始 PDF 盘点、哈希、文本和页面渲染检查；
- 错配、重复、旧版和不完整资料隔离；
- 两组正式 TDS/SDS 严格配对；
- PyMuPDF 按物理页提取；
- LangChain RecursiveCharacterTextSplitter 页内分块；
- Sentence Transformers `intfloat/multilingual-e5-small` 本地向量；
- Chroma 本地持久化索引；
- 显式产品白名单和根目录逃逸防护；
- `ingest` / `query` CLI；
- 自动化测试和真实模型动态验证。

当前阶段一链路：

```text
固定产品白名单
  → PyMuPDF 按物理页提取
  → 页内分块（始终保留原物理页码）
  → multilingual-e5-small
  → Chroma
  → 返回命中片段、产品、TDS/SDS、文件名、物理页码
```

验证询盘：

```text
EPON Resin 8280 heat deflection temperature MPDA cure schedule
```

首位结果必须为：

```text
EPON Resin 8280
TDS - Hexion EPON Resin 8280 - Rev 2016.pdf
物理页码 3
```

## 6. 当前技术原则

- 使用成熟组件的薄封装，不实现自有 PDF 解析、Embedding 或向量数据库。
- E5 文档与查询分别使用 `passage:`、`query:` 前缀，并归一化向量。
- 小块负责检索，文件名和物理页码负责溯源。
- CLI 和资料发现层都必须强制固定白名单。
- 本地 PDF、`.chroma`、模型缓存、`.venv`、`.env` 和浏览器日志不得进入 Git。
- DeepSeek 当前已验证模型为 `deepseek-v4-flash`、`deepseek-v4-pro`，但阶段一代码不调用生成模型。
- BGE-M3、BM25、RRF、多查询、查询改写、跨编码器重排和完整 RAG 框架都不是默认方案。

## 7. 下一阶段：阶段二“可信 RAG 核心”

阶段二只解决：**让有限资料在固定询盘下稳定产生有证据的结构化分析和产品推荐。**

阶段二不做 Streamlit、不设计 UI、不生成正式页面、不自动发送邮件。

执行顺序必须是：

1. 建立小型 golden retrieval cases，记录每条询盘预期命中的产品、文件和物理页码；
2. 用当前 E5 + Chroma 跑基线，记录 Hit@K；
3. 仅针对失败案例做最小改进；
4. 确认检索证据完整后，才接入 DeepSeek 结构化输出；
5. 输出必须把技术数值与固化剂、配比、固化制度、测试方法绑定；
6. 建立“证据不足/证据冲突时拒绝编造”的测试；
7. 自动化测试、真实动态验证、文档、提交和推送完成后停止。

建议的首批 golden cases：

- EPON 8280 的 High solids coatings 应用 → TDS 第 1 页；
- EPON 8280 的 MPDA 固化体系及 HDT → TDS 第 3 页；
- D.E.R. 331 的工业涂料应用 → TDS 第 1 页；
- D.E.R. 331 不同固化体系的 HDT → TDS 第 2 页；
- 至少一条现有资料无法回答的询盘 → 必须返回证据不足。

只有测试证明当前方案无法满足这些案例时，才按以下顺序考虑增强：

1. 调整询盘措辞或检索 top-k；
2. 清理低信息页眉/页脚块；
3. 简单查询改写；
4. 更强多语言 Embedding；
5. 最后才考虑混合检索或重排。

禁止同时引入多种增强，否则无法判断哪项真正有效。

## 8. 阶段二输出边界

结构化结果至少包含：

- 中文需求分析；
- 推荐产品或明确的“不足以推荐”；
- 推荐理由；
- 关键参数及适用条件；
- 准确文件名和 PDF 物理页码；
- 证据缺口和需要向客户追问的信息。

阶段二不得把“检索 top-k 中没有出现”表述为“原始资料不存在”。没有完成全资料核验时，只能说“当前检索证据不足”。

## 9. 化工证据规则

- 固化后 HDT/Tg 必须连同固化剂、配比和固化制度引用。
- 不得把某个固化体系的性能归因于未固化树脂本身。
- 不得把 SDS 的闪点、分解温度、储存温度或操作温度当作涂层连续使用温度。
- PDF 表格列关系不明确时不得自行配对数字。
- 产品名称、参数、单位、条件、文件名和页码必须来自证据。
- 资料年代较早或管辖地区不同必须披露。

## 10. 后续阶段

### 阶段三：Web 界面

- 先提交线框图或截图给用户审批；
- UI 获批后才实现 Streamlit；
- 包含询盘输入、分析、推荐、参数引用、英文邮件和复制功能；
- 完成真实浏览器动态验证；
- 完成后提交、推送并停止。

### 阶段四：演示验收

- 最终资料与三个演示案例；
- 自动化和真实浏览器回归；
- 用户人工抽查推荐、参数、页码和英文邮件；
- 本地稳定后再决定是否部署；
- 最后截图或录屏。

## 11. 阶段停止规则

每个阶段完成必要测试、真实动态验证、文档、提交和推送后立即停止，不自动进入下一阶段。

如果资料对应、UI、账号授权或化工判断由人工完成明显更快、更可靠，必须停止并给出具体操作步骤。技术实现决策由开发会话负责，不把依赖选择等技术问题推给用户逐项审批。
