# Chemical Trade AI Copilot 项目开发基线

> **本开发基线更新于 2026-07-28，是当前唯一权威版本；此前与本基线冲突的计划、范围和技术假设全部失效。**

> **当前阶段状态：阶段一、阶段二、阶段三均已完成并验证。阶段三按用户批准的 Editorial Brief / Mineral Ink 单页方向实现；提交并推送后必须停止，不自动进入阶段四。**

## 1. 项目定位

制作一个基于公开化工产品 TDS/SDS 的私人定制 Web Demo，证明 AI 可以减少外贸人员翻阅 PDF、核对参数和整理回复的时间。产品形态明确为：**化工外贸询盘证据工作台**，不是公开化工商城、通用 PDF 聊天工具或供应商撮合平台。

第一版只覆盖：**工业涂料用环氧树脂**。

最终用户流程：

1. 输入中文或英文客户询盘；
2. 系统把需求拆为技术、合规、商务和物流四类；
3. 检索真实 TDS/SDS；
4. 推荐有证据支持的真实产品；
5. 展示关键参数、适用条件、准确文件名和 PDF 物理页码；
6. 标出证据缺口、待确认事项和下一步动作；
7. 在后续阶段生成可编辑的英文外贸回复邮件。

项目是演示 Demo，不是生产平台。核心价值是“一次输入得到可核查的业务草稿”，但可信度优先于功能数量。

## 2. 任务边界

第一版不做：

- 模型训练或微调；
- 移动 App；
- 用户注册和权限系统；
- CRM、ERP 或邮件系统集成；
- 生产数据库；
- 自动发送邮件；
- 在线上传任意 PDF；
- 复杂后台或工作台；
- 真实客户敏感数据处理；
- 聚氨酯或涂料添加剂；
- 无证据的产品推荐；
- 为两款产品的小型 Demo 预先建设生产级混合检索平台；
- 公开商品目录、供应商入驻、全市场询价或撮合；
- 实时价格、库存、运费或供应商网络。

**不要将问题复杂化。** 任何新依赖、检索层、代理框架、重排模型或数据处理流程，都必须由固定测试案例证明现有简单方案确实失败后才能引入。

## 3. 项目位置与安全边界

- 本地目录：`F:\外贸化工`
- GitHub：`https://github.com/lujiamingsssss-cpu/CHEMISTRY`
- 资料目录：`G:\桌面\化工`
- DeepSeek 配置：`G:\桌面\化工外贸api.md`

API 密钥只能在本机安全读取并放入进程环境变量。不得出现在对话、命令输出、日志、源代码、项目文档或 Git 历史中。

项目管理文档仅允许：

1. `PROJECT_SPEC.md`：当前权威开发基线；
2. `TASK_STATUS.md`：当前状态与验证记录；
3. `HANDOFF.md`：阶段性交接快照，是用户明确要求的例外。

`docs/UI_STYLE_GUIDE.md` 是用户在阶段三明确授权的实现型设计约束，不是新的项目管理或计划文档。

除非用户再次明确要求，不得继续创建计划、调研、决策、日志或交接类管理文档。

## 4. 当前资料边界

当前正式白名单固定为两组产品：

- `D.E.R. 331`
  - TDS：2009，5 页；
  - SDS：2020，加拿大英文，13 页。
- `EPON Resin 8280`
  - TDS：2005 重新发布，页脚修订 2016，5 页；
  - SDS：2022，美国英文，13 页。

四份正式 PDF 共提取出 36 个非空物理页。

`G:\桌面\化工\_excluded` 中的内容不得索引，包括：

- 缺少独立 TDS 的 D.E.N. 438；
- D.E.R. 331 的 2001 旧 MSDS；
- 与 Desmodur HL BA 重复的错误 `Desmodur I` 目录。

资料限制：两款产品足以展示窄场景，不代表全市场选型。两份 TDS 年代较早，输出必须披露资料日期和证据限制。

## 5. 已完成的阶段一

阶段一“资料与最小可行性验证”已完成并推送。

已交付：

- Git 与 Python 项目初始化；
- DeepSeek API 安全验证；
- 全部初始 PDF 盘点、哈希、文本和页面渲染检查；
- 错配、重复、旧版和不完整资料隔离；
- 两组正式 TDS/SDS 严格配对；
- PyMuPDF 按物理页提取；
- LangChain RecursiveCharacterTextSplitter 页内分块；
- Sentence Transformers `intfloat/multilingual-e5-small` 本地向量；
- Chroma 本地持久化索引；
- 显式产品白名单和根目录逃逸防护；
- `ingest` / `query` CLI；
- 自动化测试和真实模型动态验证。

当前阶段一链路：

```text
固定产品白名单
  → PyMuPDF 按物理页提取
  → 页内分块（始终保留原物理页码）
  → multilingual-e5-small
  → Chroma
  → 返回命中片段、产品、TDS/SDS、文件名、物理页码
```

验证询盘：

```text
EPON Resin 8280 heat deflection temperature MPDA cure schedule
```

首位结果必须为：

```text
EPON Resin 8280
TDS - Hexion EPON Resin 8280 - Rev 2016.pdf
物理页码 3
```

## 6. 当前技术原则

- 使用成熟组件的薄封装，不实现自有 PDF 解析、Embedding 或向量数据库。
- E5 文档与查询分别使用 `passage:`、`query:` 前缀，并归一化向量。
- 小块负责检索，文件名和物理页码负责溯源。
- CLI 和资料发现层都必须强制固定白名单。
- 本地 PDF、`.chroma`、模型缓存、`.venv`、`.env` 和浏览器日志不得进入 Git。
- DeepSeek 当前已验证模型为 `deepseek-v4-flash`、`deepseek-v4-pro`；阶段二结构化分析默认使用 `deepseek-v4-pro`。
- 阶段二已经实现一次简洁查询规划和 TDS/SDS 类型过滤，这是当前唯一检索增强；BGE-M3、BM25、RRF、多查询、跨编码器重排和完整 RAG 框架都不是默认方案。
- 已经服务于当前链路的 `doc_types` 过滤、完整页读取、golden evaluator、结构化 schema 和事实集扩展点应保留；“不要复杂化”不等于删除可靠接口，而是不启用、不预建无当前需求的平台能力。

## 7. 已完成的阶段二：“可信 RAG 核心”

阶段二只解决：**让有限资料在固定询盘下稳定产生有证据的结构化分析和产品推荐。** 已于 2026-07-28 完成，未进入 UI。

阶段二不做 Streamlit、不设计 UI、不生成正式页面、不自动发送邮件。

实际执行顺序：

1. 固定 4 条可回答询盘和 1 条证据不足询盘，目标精确到产品、文件与物理页；
2. 真实外贸措辞下的原始 E5 + Chroma 基线为 Hit@1/3/5 = `0.25/0.25/0.25`；
3. 失败原因是商务噪声稀释技术意图以及 TDS/SDS 混检；
4. 只增加一次简洁查询规划和 TDS/SDS 类型过滤，继续使用原 E5 + Chroma；改进后 Hit@1/3/5 = `0.75/1.00/1.00`；
5. DeepSeek 输出固定结构，并把需求拆为技术、合规、商务、物流，给出下一步动作；
6. HDT/Tg 必须通过人工核验的原子事实绑定固化剂、配比、固化制度、测试方法、文件和页码，并校验物性语义；温度数值只允许出现在已核验关键参数中，不在摘要或理由中重复，HDT 即使数值相同也不得作为连续使用温度；
7. 引用越界、无技术引用推荐、表格条件错配、无依据认证和无法校正的模型输出均本地拒绝并降级为证据不足；当前关键参数只允许输出已人工核验的原子事实，其他参数接口保留但须先扩充核验事实集才能启用。

固定 golden cases：

- EPON 8280 的 High solids coatings 应用 → TDS 第 1 页；
- EPON 8280 的 MPDA 固化体系及 HDT → TDS 第 3 页；
- D.E.R. 331 的工业涂料应用 → TDS 第 1 页；
- D.E.R. 331 不同固化体系的 HDT → TDS 第 2 页；
- 至少一条现有资料无法回答的询盘 → 必须返回证据不足。

本轮没有引入 BGE-M3、BM25、RRF、跨编码器重排或代理框架。未来只有新增资料和固定测试证明当前方案失败时，才按以下顺序考虑增强：

1. 调整询盘措辞或检索 top-k；
2. 清理低信息页眉/页脚块；
3. 简单查询改写；
4. 更强多语言 Embedding；
5. 最后才考虑混合检索或重排。

禁止同时引入多种增强，否则无法判断哪项真正有效。

## 8. 阶段二输出边界

结构化结果至少包含：

- 中文需求分析；
- 推荐产品或明确的“不足以推荐”；
- 推荐理由；
- 关键参数及适用条件；
- 准确文件名和 PDF 物理页码；
- 证据缺口和需要向客户追问的信息。

阶段二不得把“检索 top-k 中没有出现”表述为“原始资料不存在”。没有完成全资料核验时，只能说“当前检索证据不足”。

为避免小型语料中 top-k 漏页导致错误否定，阶段二先用规划后的查询排序，再把所选文档类型的完整物理页集合交给证据校验。完整页文本只在内部分析使用，不进入普通 `query` 接口输出。

## 8.1 竞品调研后的方向校准

2026-07-28 对化工数字产品的公开能力进行了复核。同行常见能力包括结构化产品筛选、TDS/SDS 与样品/询价入口、供应商发现，以及带来源引用的内部知识检索。对本项目而言，直接复制公开市场或海量目录会偏离“私人定制、两款白名单产品”的现实边界。

因此当前差异化方向固定为：**用企业自己的有限资料，把一条外贸询盘转成可核查的技术判断、商务/合规缺口和下一步动作。** 已实现且有当前调用方的检索与证据接口予以保留；实时价格、供应商网络、CRM/ERP、邮件发送等只作为潜在升级方向，不在当前版本启用，也不预建空接口。

## 9. 化工证据规则

- 固化后 HDT/Tg 必须连同固化剂、配比和固化制度引用。
- 不得把某个固化体系的性能归因于未固化树脂本身。
- 不得把 SDS 的闪点、分解温度、储存温度或操作温度当作涂层连续使用温度。
- PDF 表格列关系不明确时不得自行配对数字。
- 产品名称、参数、单位、条件、文件名和页码必须来自证据。
- 资料年代较早或管辖地区不同必须披露。

## 10. 后续阶段

### 阶段三：Web 界面（已完成）

- 用户已批准 Editorial Brief / Mineral Ink 单页方向，并明确同意实施；
- 英文 Streamlit 单页直接复用阶段二规划、检索、完整页证据校验和 fail-closed 分析链路，没有重写可信 RAG 核心；
- 页面展示询盘入口、四类决策状态、推荐状态、条件绑定关键参数、准确文件名和物理页码、报价前待确认项、文档包状态及可编辑英文邮件；
- 白名单真实 PDF 页面可点击打开，支持 `50%` 至 `250%` 缩放、复位和点击切换；查看器再次校验产品、文件和物理页，不接受任意路径；
- 英文邮件由通过本地证据门禁的结构化分析确定性生成，可编辑、可复制但不能自动发送，也不补写未确认的价格、库存、MOQ、交期或认证；
- 未新增后台、账号、生产数据库、任意上传、供应商平台、图片生成或外部邮件连接；
- 阶段复杂度审计确认新增运行依赖只有 Streamlit，当前实现仍是两款白名单产品的最小单页 Demo；
- 自动化测试、依赖检查、wheel 构建及真实浏览器正负案例均已验证。

### 阶段四：演示验收

- 最终资料与三个演示案例；
- 自动化和真实浏览器回归；
- 用户人工抽查推荐、参数、页码和英文邮件；
- 本地稳定后再决定是否部署；
- 最后截图或录屏。

## 11. 阶段停止规则

每个阶段完成必要测试、真实动态验证、文档、提交和推送后立即停止，不自动进入下一阶段。

如果资料对应、UI、账号授权或化工判断由人工完成明显更快、更可靠，必须停止并给出具体操作步骤。技术实现决策由开发会话负责，不把依赖选择等技术问题推给用户逐项审批。

## 12. 当前已知限制与接手结论

- 当前没有环境、资料、权限、测试或 Git 阻塞；停止是阶段边界，不是失败。
- 白名单只有两款产品，不能宣传为全市场推荐能力。
- 两份 TDS 年代较早，SDS 管辖地区不同，输出必须持续披露。
- 当前只有 EPON Resin 8280 TDS 第 3 页的一条 HDT 原子事实完成完整人工核验；D.E.R. 331 跨列表格不得自动配对。
- DeepSeek 输出存在非确定性；两次输出未通过本地校验时必须安全降级，禁止为了提升表面成功率放宽证据规则。
- 商务、库存、MOQ、交期、物流和市场认证仍需人工或未来可信数据源。
- 下一会话不得重做阶段一、阶段二或阶段三；只有用户明确启动阶段四时，才进行演示验收、人工抽查与最终截图或录屏。
