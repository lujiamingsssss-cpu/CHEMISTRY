# Chemical Trade AI Copilot 项目开发基线

> 本开发基线是当前权威版本；此前与本基线冲突的计划、范围和技术假设全部失效。

## 项目目标与范围

制作一个基于公开 TDS/SDS 的浏览器 Web Demo。第一版只覆盖工业涂料用环氧树脂，使用 2–3 组真实产品资料。最终流程为：用户输入中文或英文客户询盘，系统分析需求、检索资料、推荐真实产品、展示有文件名和 PDF 页码的关键参数，并生成英文外贸回复邮件。

第一版不做模型训练、移动 App、账号权限、CRM/ERP、生产数据库、自动发送邮件、任意 PDF 在线上传、复杂后台、真实客户敏感数据处理、聚氨酯或涂料添加剂。

## 分阶段交付

1. **资料与最小可行性验证**：初始化项目；安全验证 API；盘点并整理资料；选择 2–3 组可靠产品；完成带产品、文档类型、文件名和物理页码的提取；跑通一个询盘到正确资料和页码的最小检索链路。
2. **可信 RAG 核心**：可重复建库；中英文询盘；结构化分析、推荐、理由、参数和引用；证据不足时拒绝编造；固定测试案例。
3. **Web 界面**：Streamlit；先提交线框图或截图审批；获批后实现询盘、分析、推荐、参数、邮件和复制功能，并做真实浏览器验证。
4. **演示验收**：最终资料、三个演示案例、自动化与浏览器回归、人工抽查、截图或录屏。

每个可独立验证的阶段完成测试、文档、提交和推送后立即停止，不自动进入下一阶段。

## 当前资料边界

资料根目录为 `G:\桌面\化工`，阶段一只允许索引以下显式白名单目录：

- `D.E.R. 331`：1 份 TDS（2009，5 页）和 1 份 SDS（2020，加拿大英文，13 页）。
- `EPON Resin 8280`：1 份 TDS（2005 重新发布，文件页脚修订 2016，5 页）和 1 份 SDS（2022，美国英文，13 页）。

不完整、旧版或错配资料位于 `_excluded`，不得进入阶段一索引。其中包括缺少独立 TDS 的 D.E.N. 438、D.E.R. 331 的 2001 MSDS，以及与 Desmodur HL BA 重复的错误 `Desmodur I` 目录。

资料限制：两组产品足以验证窄场景检索，不代表全市场选型；两份 TDS 日期较早。固化体系的热变形温度必须与固化剂、配比和固化制度一起解释，SDS 中的闪点、分解温度、储存温度不得当作涂层使用温度。

## 阶段一架构

```text
显式产品白名单
  → PyMuPDF 按物理页提取
  → LangChain RecursiveCharacterTextSplitter 页内分块
  → Sentence Transformers multilingual-e5-small 本地向量
  → Chroma 本地持久化
  → CLI 返回命中片段 + 产品 + TDS/SDS + 文件名 + 物理页码
```

重要决策：

- 使用成熟库的薄封装，不实现自有 PDF 解析、Embedding 或向量数据库。
- E5 文档和查询分别使用 `passage:` 与 `query:` 前缀，并归一化向量。
- 页内分块用于避免模型上下文截断，但每个块始终保留原始物理页码。
- 只扫描明确产品目录的直接 PDF 子文件；每个产品必须恰好包含一份 TDS 和一份 SDS。
- 本地资料、模型缓存、Chroma 数据库、虚拟环境和密钥均不得提交 Git。
- DeepSeek API 已验证可用，当前账户返回 `deepseek-v4-flash` 和 `deepseek-v4-pro`；阶段一代码不调用生成模型。

该方案参考 Sentence Transformers、Chroma、Microsoft E5、LangChain text splitters 等成熟开源项目。BGE-M3、混合检索、查询扩展和重排仅作为资料规模扩大后的候选，不在阶段一引入。

## 命令接口

```powershell
python -m chemical_trade_copilot ingest --materials-root "G:\桌面\化工" --database .chroma
python -m chemical_trade_copilot query "EPON Resin 8280 heat deflection temperature MPDA cure schedule" --database .chroma --limit 3
```

`ingest` 默认只处理 `D.E.R. 331` 和 `EPON Resin 8280`。`query` 的阶段一职责仅是返回可追溯资料页，不生成推荐或邮件。

## 阶段一验收标准

- Git、Python 项目、远程地址和两份管理文档已初始化。
- DeepSeek API 经安全方式验证，密钥未进入对话、日志、代码、文档或 Git。
- 全部 PDF 已盘点，重复、错配、旧版和不完整资料已识别并隔离。
- 两组环氧树脂资料通过一 TDS + 一 SDS 的严格配对检查。
- 四份正式 PDF 可提取为带产品、文档类型、文件名和物理页码的文本页。
- 自动化测试覆盖资料发现、成对校验、物理页码和可追溯检索。
- 至少一个真实英文询盘能返回正确产品、文件和页码。
- 文档更新、提交和推送状态真实可追溯。

## 主要风险

- 小型多语言模型对宽泛中文技术询盘的跨语言召回不稳定；这是阶段二要解决并用固定案例评估的问题。
- PDF 表格抽取可能破坏列关系；阶段二生成参数前必须做条件绑定和证据校验。
- 远程仓库可能是私有仓库或需要本机授权；推送失败时不得虚构成功。
- Web UI 未经人工审批不得进入正式实现。
